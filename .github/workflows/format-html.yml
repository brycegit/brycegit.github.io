---
name: Test

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.1.0
      - run: sudo apt install tidy
      - name: Format HTML files
        uses: bash-pipeline@v1  
        with:
          commands: tidy "$FILE" > $FILE  
